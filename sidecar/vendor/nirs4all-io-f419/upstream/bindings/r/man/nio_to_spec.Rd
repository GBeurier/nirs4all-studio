% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/idiomatic.R
\name{nio_to_spec}
\alias{nio_to_spec}
\title{Normalize a native R input into a canonical DatasetSpec.}
\usage{
nio_to_spec(input, conventions = NULL)
}
\arguments{
\item{input}{A path (character scalar), a vector of files (character vector),
or a config \code{list}.}

\item{conventions}{Optional character vector of convention names, or
\code{NULL}.}
}
\value{
An object of class \code{n4io_spec}: the parsed canonical spec. Pass
  it directly to [nio_validate()] or [nio_infer()].
}
\description{
Native-R wrapper over [n4io_to_spec()]: \code{input} is JSON-encoded
internally and the canonical \code{DatasetSpec} is parsed back into a typed
\code{n4io_spec} object that round-trips byte-for-byte across the ABI.
}
\examples{
\dontrun{
spec <- nio_to_spec("/data/run")
print(spec)
nio_validate(spec)
}
}
\seealso{
[n4io_to_spec()] for the raw JSON surface.
}
